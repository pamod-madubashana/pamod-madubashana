# Waka Readme — disabled
# Replaced by dynamic SVG card in README.md (github-stats-extended WakaTime card).
# No workflow needed: card renders live via https://github-stats-extended.vercel.app/api/wakatime
# This file is kept as a no-op placeholder to avoid breaking references; safe to delete.
name: Waka Readme (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "WakaTime card is now dynamic SVG — no workflow required."
